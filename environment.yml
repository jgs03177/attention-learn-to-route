# run: conda env create --file environment.yml
name: attention_tsp
channels:
- pytorch
dependencies:
- python>=3.8
- anaconda
- tqdm
- pytorch
- torchvision
- cuda91
- pip
- pip:
  - protobuf==3.20.3
  - tensorboard_logger